import type { AppConfig } from '../../../../config/types'

export function registerDatabaseIpc(_config: AppConfig) {
  // Database IPC contracts will be narrowed as repositories and services are introduced.
}
